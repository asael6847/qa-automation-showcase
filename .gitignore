# Dependencias
node_modules/
.pnpm-store/

# Builds
dashboard/.next/
dashboard/out/
**/dist/

# Playwright
tests/test-results/
tests/playwright-report/
tests/blob-report/
tests/.playwright/

# Reportes generados en cada corrida (los de ejemplo viven en dashboard/public/runs y SÍ se commitean)
tests/reports/

# Evidencia generada (PDF de pasos + su reporte HTML). La carpeta `evidencias/`
# de la raíz se versiona (con .gitkeep) pero sus PDF no.
tests/evidence/
tests/evidence-report/
/evidencias/*
!/evidencias/.gitkeep

# Backend Python / uv
backend/.venv/
**/__pycache__/
*.pyc
backend/.pytest_cache/
backend/.ruff_cache/

# Entorno
.env
.env.local
*.log

# OS / editores
.DS_Store
.idea/
.vscode/

# Markdown / documentación (no se sube al repo)
*.md
*.MD
